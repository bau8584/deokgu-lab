# 공통 작업 원칙 (항상 우선 — 세션 회고로 도출)

아래 개별 지침(캡처·소개글·사이트 빌드 등)보다 **이 원칙이 우선**한다.

## 1. 쓰기 전에 "독자·목적"부터 못 박기
게시글·카피·가이드를 쓰기 전에 먼저 한 줄씩 적고 시작한다. 애매하면 바로 쓰지 말고 확인받는다.
- **독자**: 정확히 누구인가 (예: 배포를 *받아서 적용하는* 교사 / 동료 초등교사)
- **목적**: 이 글이 하려는 것 한 줄
- **이건 아님**: 헷갈리기 쉬운 범위 밖 (예: "도구 자랑이 아니라 배포 방법")
> 배경: 앱스크립트 팁이 독자·목적을 안 정하고 시작해 4번 엎어졌다(선거중심→예시일뿐 / 배포자→수신자 / 학교전용→반·개인용도).

## 2. 정체성·브랜드 요소는 단독 결정 금지 — 선택지를 준다
캐치프레이즈·태그라인·톤·네이밍처럼 **브랜드 정체성에 닿는 결정은 내가 정하지 않는다.** 후보 2~3개를 제시하고 소유자가 고르게 한다. (팔레트·폰트 등 이미 확정된 것은 그대로 적용.)
> 배경: "티칭에서 코칭으로" 대체 문구를 내가 단독으로 정함 → 소유자가 직접 고르길 원함.

## 3. 시각 버그 피드백은 "원인 특정" 후 수정
"버튼이 없다/깨진다" 같은 리포트는 내 해석대로 바로 고치지 말고, **실제 렌더를 비교해 원인을 먼저 짚는다.** (예: 배지가 스크린샷에 가려짐 = z-index 문제.) 엉뚱한 걸 고치는 걸 방지.

## 4. "빼/바꿔" 지시는 이후 산출물에서도 유지 확인
문구 삭제·변경을 지시받으면, 이후(특히 서브에이전트가) 글을 다시 쓸 때 그 변경이 되살아나지 않았는지 `grep`으로 확인한다.
> 배경: 삭제한 "티칭에서 코칭으로"가 재작성 때 되살아남.

## 5. 개인정보 (재확인·강화)
실명이 보이면 블러하되 **반·번호 같은 준식별자까지** 가린다. 비밀번호 등 민감 탭·화면은 열지 않는다. 가능하면 **데모(가짜 이름) 데이터**로 캡처하고, 공개 전 캡처를 육안으로 재확인한다.

---

# 스크린샷 캡처 작업 지침

이 파일은 "특정 URL의 스크린샷을 뽑아줘 / 캡처해줘 / 화면 떠줘" 류의 요청을
받았을 때 **매번 동일하게** 따라야 할 절차다. 사용자가 일일이 세부 지시를 하지
않아도 아래 규칙대로 자동으로 수행한다.

## 트리거
사용자가 URL을 주며 "스크린샷 / 캡처 / 화면" 등을 언급하면 이 워크플로를 실행한다.

## 1. 출력 폴더 생성
- `shots/<슬러그>-<YYYY-MM-DD>/` 형식으로 새 폴더를 만든다.
- 슬러그: 앱 이름이 분명하면 그걸로, 아니면 URL 호스트에서 추출
  (예: `pe-scoreboard.pages.dev` → `pe-scoreboard`).
- 같은 날 같은 앱을 다시 찍으면 `-2`, `-3`을 붙여 **기존 폴더를 덮어쓰지 않는다.**

## 2. 환경 준비
- Playwright 미설치 시 설치한다: `npm i -D playwright && npx playwright install chromium`
- 캡처 스크립트는 `scripts/capture.mjs`로 저장해 재사용한다(있으면 재사용).
- Windows 파이썬은 스토어 스텁이라 못 쓴다 → **node/Playwright만** 사용. 이미지 블러도 파이썬(Pillow) 대신 Playwright(캡처 직전 CSS blur, 또는 정적 이미지엔 `scripts/_blur.mjs`)로 처리.

## 2-B. 로그인이 필요한 앱 캡처 — CDP 세션 재사용 (확정 방법)
Playwright 헤드리스는 구글 로그인 앱에 못 들어가고, Claude-in-Chrome 스샷은 repo로 파일 저장이 안 된다. **소유자가 이미 로그인해 둔 브라우저를 디버그 포트로 재사용**한다:
1. 브라우저 완전 종료(백그라운드 프로세스까지) 후 `msedge --remote-debugging-port=9222 --user-data-dir="<실제 User Data>" --restore-last-session`로 재시작(로그인·탭 유지). 최신 크롬/엣지는 `--user-data-dir`을 명시해야 디버그 포트가 열린다.
2. `chromium.connectOverCDP('http://localhost:9222')` → `contexts()[0].newPage()`로 캡처. 끝에 `browser.close()`는 연결만 끊김(브라우저 안 죽음).
3. **토큰 읽기·프로필 복사는 금지**(보안 분류기가 차단, 우회 안 함). 앱마다 Supabase 세션이 origin별로 달라 각각 로그인 필요.
- 도구: `scripts/cdp-cap.mjs`(연결·로그인 확인 패턴). 데모(가짜 이름) 리그·데이터 상태로 찍는다.

## 3. 캡처할 기능 파악 — 빠짐없이
- 이 저장소에 소스가 있으면 **먼저 코드를 읽는다.** 라우트·컴포넌트·주요
  인터랙션을 근거로 핵심 기능 목록을 만든다.
- 소스가 없으면 페이지 DOM과 주요 섹션을 탐색해 기능 블록을 식별한다.
- 모달·탭·토글처럼 **클릭해야 보이는 상태도 빠뜨리지 않는다.** 각 상태를 별도 컷으로.
- 캡처 전, "이 N개 화면을 찍을 예정"이라고 목록을 한 번 보여준 뒤 진행한다.

## 4. 캡처 규칙 — 요소 단위로 타이트하게 크롭
- 전체 화면(`page.screenshot`)이 아니라 **각 기능 UI 요소에 딱 맞게 크롭**한다.
- 기본은 `boundingBox` + `clip`으로 **여백 16px**만 두고 자른다(쓸데없는 부분 제거).
- `deviceScaleFactor: 2`로 선명하게 캡처한다.
- 뷰포트 기본값은 1440x900. 앱이 태블릿/모바일 중심이면 맞게 조정하고 사용자에게 알린다.
- 각 컷은 `networkidle` 대기 + 대상 요소 `visible` 대기 후 촬영한다.

핵심 코드 패턴(이 방식을 사용한다):
```js
const ctx = await browser.newContext({ viewport: { width:1440, height:900 }, deviceScaleFactor: 2 });
const el = page.locator(selector).first();
await el.scrollIntoViewIfNeeded();
await el.waitFor({ state: 'visible' });
const b = await el.boundingBox();
await page.screenshot({ path: file, clip: {
  x: Math.max(0, b.x-16), y: Math.max(0, b.y-16),
  width: b.width+32, height: b.height+32,
}});
```

## 5. 파일 정리 — 보기 좋게
- 파일명: `01-<기능명>.png`, `02-...` 처럼 **번호 + 영문 케밥케이스 기능명.**
- 폴더 안에 `README.md` 인덱스를 만든다. 각 파일에 대해
  파일명 / 한 줄 설명 / 미리보기(`![](파일명)`)를 표로 정리한다.
- 마지막에 캡처 장수, 폴더 경로, 누락 가능성이 있는 화면을 요약 보고한다.

## 6. 안전·예외 처리
- 로그인이 필요하면 진행 전에 사용자에게 입력 방법을 묻는다. **비밀번호를 코드에
  하드코딩하지 않는다.**
- 학생 이름 등 개인정보가 보이면 블러 처리 여부를 사용자에게 확인한다.
- 셀렉터를 못 찾은 기능은 조용히 건너뛰지 말고 **실패 목록으로 보고**한다.

## 기본값 요약
| 항목 | 값 |
|------|----|
| 뷰포트 | 1440 x 900 |
| 선명도 | deviceScaleFactor 2 |
| 크롭 여백 | 16px |
| 출력 폴더 | `shots/<슬러그>-<날짜>/` |
| 파일명 | `NN-기능명.png` |

---

# 주석·강조 작업 지침

"이 폴더 스샷에 주석/강조 달아줘" 요청을 받거나, 캡처 직후 이어서 수행한다.

## 도구
- Python + Pillow로 그린다. 한글 라벨이 깨지지 않도록 **한글 지원 폰트**를 쓴다
  (Noto Sans KR 또는 Pretendard). 없으면 설치한다 (예: `apt-get install -y fonts-noto-cjk`).
- **원본은 보존**한다. 결과는 `<원본이름>-annotated.png`로 따로 저장한다.
- 원본 해상도(2x)를 유지해 선명도를 잃지 않는다.

## 비주얼 톤앤매너 (박덕구쌤 브랜드, 고정)
- 무드: **밝고 유쾌하게 시선 끌기.** 또렷하되 공격적이지 않게, 따뜻한 강조.
- 강조색: 메인 `#F2A33C`(치즈 오렌지), 포인트 `#3CC9A0`(민트). 텍스트는 흰색 또는 차콜 `#2E2A26`.
- 화살표: 두께 8px, 큰 화살촉, 둥근 끝. 도드라지되 부드럽게.
- 강조 박스: 라운드 사각형 테두리 6px(치즈 오렌지). 필요 시 바깥을 25% 어둡게(스포트라이트).
- 라벨 칩: 치즈 오렌지 채움 + 흰 글씨 + 둥근 모서리. 핵심 1개는 민트로 변주 가능.
- 번호 배지: 원형 치즈 오렌지 배경에 흰 숫자.
- 시그니처: 가끔 한 컷에 치즈냥 스티커를 작게 얹어도 좋다(과하지 않게, 중간 노출).

## 라벨 텍스트
- **짧은 설명형**으로 쓴다. 예: "탭 한 번으로 기록", "실시간으로 점수 반영", "1초 만에 팀 편성".
- 한 라벨은 12자 내외. 기능의 *가치*를 한 줄로.

## 규칙
- 이미지당 강조 포인트는 **1~3개**만. 과하게 달지 않는다(시선 분산 방지).
- 강조할 위치와 라벨 문구를 먼저 목록으로 제안하고, 사용자 확인 후 그린다.
- 학생 이름 등 개인정보가 보이면 같은 작업에서 블러 처리한다.

## 정리
- README 인덱스에 주석본(`-annotated.png`) 행도 추가한다.
- 끝에 처리한 장수와 각 이미지에 단 라벨 문구를 요약 보고한다.

---

# 소개 글 작업 지침

"이 앱/이 폴더로 소개글 써줘" 요청을 받으면 수행한다.

## 입력 파악
- 앱 소스가 있으면 읽어 핵심 기능과 *해결하는 문제*를 파악한다.
- `shots/` 폴더에 캡처·주석본이 있으면, 어떤 화면을 글에 붙일지 매칭한다.
- 불명확하면 한두 가지만 사용자에게 확인한다. **없는 기능은 지어내지 않는다.**

## 스타일 (박덕구쌤 페르소나, 고정)
- 화자: **박덕구쌤** — "옆 반에 늘 뭔가 만드는 유쾌한 쌤." 밝고 친근, 가끔 셀프 디스
  유머("또 만들었습니다… 누가 말려요 😅"). 가르치려 들지 않고 선물처럼 툭 건넨다.
- 톤: 후킹은 살리되 **따뜻하게.** 짧은 문장, 줄바꿈 시원하게, 스크롤하며 스캔되게.
  과장 광고 금지 — 효과는 솔직하게.
- 독자: **동료 초등교사.** 모든 가치를 "교실에서 뭐가 줄어드나 / 수업이 어떻게
  편해지나"로 번역한다. ("손 덜 가는 교실" — 교사 본인이 얼마나 편해지는지를 가치로)
- 이모지·해시태그 적극 사용(과하지 않게). 치즈냥 🐈을 가끔 시그니처로.
  기본 해시태그(앱에 맞게 가감): #초등교사 #교실수업 #수업나눔 #에듀테크 #박덕구쌤
- 마무리 시그니처(선택): "만드는 재미로 사는 초등쌤, 박덕구 🐈"

## 구조 (어텐션 공식)
1. **훅** — 첫 줄에서 공감/문제 찌르기 (예: "○○하느라 쉬는 시간 또 날아갔죠? 😮‍💨")
2. **비포 → 애프터** — 원래 이렇게 번거롭던 게 → 이렇게 바뀐다
3. **핵심 기능 2~3개** — 각 한 줄, 기능이 아니라 *가치* 중심
4. **CTA** — 써보기 링크 / "저장해두세요" / "동학년쌤 태그하기"

## 출력 — 3가지 버전을 한 번에
- **인스타·틱톡 캡션**: 가장 짧고 후킹 강하게. 첫 1~2줄에서 승부. 줄바꿈·이모지·해시태그 풀로.
- **포트폴리오·블로그**: 중간 길이. 맥락 + 기능 + 스샷 배치 제안(`[01-home.png]` 위치 표시).
- **초등교사 사이트**: 정보 충실형. 어떤 학년·교과·수업 상황에서 쓰면 좋은지까지.
  이 버전만 후킹을 살짝 눌러 **신뢰감 있게** 쓴다.

## 정리
- 결과는 `shots/<해당폴더>/copy.md`로 저장하고, 세 버전을 헤더(`## 인스타`, `## 블로그`,
  `## 교사 사이트`)로 구분한다.
- 끝에 어떤 캡처를 어느 위치에 넣으면 좋을지 한 줄로 제안한다.

---

# 포트폴리오 페이지 작업 지침

"이 앱들 모아서 포트폴리오 페이지 만들어줘 / 갱신해줘" 요청을 받으면 수행한다.

## 결과물 형태
- **단일 HTML 파일**(`index.html`). CSS·JS 인라인. Cloudflare Pages에 그대로 배포 가능.
- 외부 의존성 최소화(폰트만 CDN 허용).

## 데이터 소스 — 자동 수집
- `shots/` 아래 각 앱 폴더를 훑어, 대표 썸네일(주석본 `-annotated.png` 우선,
  없으면 첫 캡처)과 `copy.md`의 **블로그 버전**을 카드 내용으로 가져온다.
- 앱별 링크는 폴더의 메타나 사용자 입력에서 받는다. 없으면 비워두고 보고한다.

## 레이아웃 — 카드 그리드 (확정)
- 반응형 카드 그리드(`repeat(auto-fit, minmax(240px, 1fr))`).
- 카드: 썸네일(위) + 제목 + 한 줄 소개 + "써보기" 링크 버튼.
- 상단 헤더: 워드마크 "박덕구쌤" + **치즈냥 짝꿍 아이콘(중간 노출)** + 태그라인
  "만드는 재미로 사는 초등쌤, 박덕구".

## 디자인 — 치즈 팔레트 (확정)
- 배경 크림 `#FFF7EC`, 카드 흰색 계열, 텍스트 차콜 `#2E2A26`.
- 강조(버튼·링크·헤더 포인트) 치즈 오렌지 `#F2A33C`, 포인트 민트 `#3CC9A0`.
- 둥근 모서리, 부드러운 그림자. 폰트는 둥근 산세리프(제목)+가독성 산세리프(본문).
- 모바일에서 1열로 자연스럽게 무너지게.

## 규칙
- 새 앱이 `shots/`에 추가되면 **카드 한 장만 더**하는 방식으로 갱신(전체 재생성 지양).
- 사실만. `copy.md`에 없는 기능을 지어내지 않는다.
- 완성 후 파일 경로를 안내하고, 배포 전 점검 포인트(링크·이미지 경로)를 한 줄로 보고한다.

---

# 사이트 빌드 (deokgu-lab 포트폴리오 홈)

## 개요
`index.html`은 자리표시(placeholder)가 있는 완성 초안이다. 필터(도구·팁·실험실·뀨),
정렬(추천·최신·인기), 카드별 "도움됐어요" 버튼, 개선·문의 제안함이 UI로 구현돼 있고,
좋아요/제안은 아직 in-memory(하드코딩)다. 실물 자산 교체 + 백엔드 연결 + 배포가 남았다.

## 브랜드 (항상 참조)
`@docs/박덕구쌤-브랜드가이드.md` 의 톤·팔레트·북극성을 따른다.
북극성: **"재미가 의무 되면 안 됨."** 기능이 유지보수 부담·의무를 만들면 경계한다.
팔레트: 치즈 `#F2A33C` / 크림 `#FFF7EC` / 차콜 `#2E2A26` / 민트 `#3CC9A0`.
폰트: 손글씨 Gaegu(로고·제목), 본문 Gowun Dodum.

## 폴더
```
deokgu-lab/
├─ CLAUDE.md
├─ index.html
├─ assets/   # kkyu-base.png(헤더 얼굴), logo.png, stickers/
├─ shots/    # capture.mjs 결과 (앱별 폴더)
├─ docs/     # 브랜드가이드·작품인벤토리·뀨 캐릭터 브리프
└─ scripts/capture.mjs
```

## 스택
정적 HTML → Cloudflare Pages 배포. 백엔드는 Supabase (좋아요·제안함). 리그 앱과 동일 스택.

## 할 일 (우선순위)
1. **자리표시 교체**
   - 헤더 SVG 뀨 → `assets/kkyu-base.png` (이모티콘 '기본' 얼굴)
   - 각 카드 "스크린샷 자리" → `shots/`의 실제 스크린샷
   - 카드 링크·푸터 링크(`#`) → 실제 URL (소유자가 제공)
2. **Supabase 연결**
   - `likes(item_id text primary key, count int default 0)` — 카드 "도움됐어요"
   - `feedback(id, item_id, message, contact null, created_at)` — 제안함(insert-only)
   - RLS: 익명 insert/증가 허용, `feedback` 읽기는 소유자만. `likes` count 공개 읽기 OK.
   - 좋아요 클릭 → count +1 (RPC/Edge Function). 중복은 `localStorage`. 어뷰징은 IP 레이트리밋.
   - Supabase URL/anon key는 환경변수. (프로젝트 생성·키 발급은 소유자가.)
3. **Cloudflare Pages 배포.**
4. (선택) 작품 상세/모달 + 제안함 연결 — 방식은 소유자 확정 후.

## 규칙
- 되돌리기 힘든 작업(배포·삭제·권한 변경)은 소유자에게 먼저 확인.
- 새 기능이 "정기적으로 채워야 하는 의무"를 만들면, 만들기 전에 소유자에게 그 부담을 짚어준다.
